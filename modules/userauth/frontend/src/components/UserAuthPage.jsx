/**
 * Placeholder component — replace with auth UI (login, register, etc.).
 */
export default function UserAuthPage() {
  return (
    <section className="section">
      <div className="container">
        <h1 className="title">User Authentication</h1>
        <p className="subtitle">Module installed. Authentication UI goes here.</p>
      </div>
    </section>
  );
}
