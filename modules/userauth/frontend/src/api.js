/**
 * API calls for the userauth module.
 * Re-exports the core client so module components have a single clean import.
 * Add module-specific API functions below as needed.
 */
export { get, post, put, del } from '@core/frontend/api';
